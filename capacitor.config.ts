import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.magnifiq.services',
  appName: 'Magnifiq Services',
  webDir: 'dist'
};

export default config;
